export type multiCurrency = {
  currency: string
  income: number
  expense: number
  balance: number
}

export interface DataItem {
  currency: string;
  income: number;
  expense: number;
  balance: number;
}